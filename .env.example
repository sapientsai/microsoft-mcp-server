# Azure App Registration
AZURE_CLIENT_ID=your-azure-client-id
AZURE_CLIENT_SECRET=your-azure-client-secret
AZURE_TENANT_ID=common

# Auth mode: 'interactive' (default) or 'clientCredentials'
# - interactive: OAuth 2.0 authorization code flow (requires user login)
# - clientCredentials: OAuth 2.0 client credentials flow (app-only, no user)
AZURE_AUTH_MODE=interactive

# Server Configuration
BASE_URL=http://localhost:8080
PORT=8080

# Transport: httpStream (default) or stdio
TRANSPORT_TYPE=httpStream

# MCP endpoint protection (optional)
# When set, requests must include "Authorization: Bearer <key>" header
MCP_API_KEY=

# Graph API Scopes for interactive mode (comma-separated)
# Read-only (default):
# GRAPH_SCOPES=openid,profile,email,User.Read,Mail.Read,Calendars.Read,Files.Read,Sites.Read.All
# With write access:
# GRAPH_SCOPES=openid,profile,email,User.Read,Mail.ReadWrite,Calendars.ReadWrite,Files.ReadWrite.All,Sites.ReadWrite.All

# App-only scopes for client credentials mode (comma-separated)
# Default: https://graph.microsoft.com/.default
# For Azure Management API, add: https://management.azure.com/.default
GRAPH_APP_SCOPES=https://graph.microsoft.com/.default

# Azure AI Search (optional — tool only appears when all three required vars are set)
# See docs/AZURE-AI-SEARCH.md for full setup guide
# AZURE_AI_SEARCH_ENDPOINT=https://my-search.search.windows.net
# AZURE_AI_SEARCH_API_KEY=your-query-api-key
# AZURE_AI_SEARCH_INDEX_NAME=my-index
# AZURE_AI_SEARCH_SEMANTIC_CONFIG=my-semantic-config
# AZURE_AI_SEARCH_VECTOR_FIELDS=contentVector
# AZURE_AI_SEARCH_SELECT_FIELDS=title,content,url
