# Azure App Registration (required for device_code and client_credentials modes)
AZURE_CLIENT_ID=your-client-id-here
AZURE_TENANT_ID=common

# Authentication Mode: device_code | client_credentials | client_token
# - device_code: Interactive sign-in via browser (default)
# - client_credentials: App-only auth with client secret
# - client_token: Manual token provided via ACCESS_TOKEN or set_access_token tool
AUTH_MODE=device_code

# For client_credentials mode only
# AZURE_CLIENT_SECRET=your-client-secret-here

# For client_token mode - pre-obtained access token
# ACCESS_TOKEN=your-access-token-here

# Graph API Configuration
GRAPH_API_VERSION=v1.0
# GRAPH_SCOPES=User.Read,Mail.Read,Calendars.Read
